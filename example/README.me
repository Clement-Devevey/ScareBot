Dans ce dossier, voici un example de menu pour l'ajout d'un jeu.

1) Mettre les 3 fichiers python dans rootfs_overlay/game/
2) Changer dans etc/init.d/S01game la dernière ligne par python3 start.py

Maintenant la console boot sur un écran vert avec un curseur qui se déplace en haut et en bas. En validant le choix du haut, on lance la scarebot. 
En quittant le jeu scarebot on retourne bien sur le menu.

A rajouter : le titre de chaque menu (Scarebot, ...)
